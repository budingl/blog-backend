# 本地开发环境专属配置
spring:
  # 本地数据库配置
  # 用于本地通过SSH隧道连接服务器中Docker里的PostgreSQL数据库
  datasource:
    url: jdbc:postgresql://localhost:5432/blog
    username: xiaojiu
    password: xiaojiu
  # 本地调试配置
  devtools:
    restart:
      enabled: true  # 热部署（本地开发用）

# 本地日志配置（更详细）
logging:
  level:
    com.example: DEBUG  # 本地调试开启 DEBUG 日志
    root: DEBUG

# SSH隧道设置（用于参考）
# 运行以下命令创建SSH隧道：
# ssh -L 5432:1Panel-postgresql-eMzN:5432 xiaojiu@118.26.39.65
# 这将把本地5432端口转发到服务器上的PostgreSQL容器的5432端口
